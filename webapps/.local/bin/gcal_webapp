#!/bin/bash
# Webapp launcher med Thorium
URL="https://calendar.google.com"
PROFILE="$HOME/.config/webapps/calendar"
CLASS="CalendarApp"

# Vi bruker --class så dwm-regelen din treffer
thorium-browser --app="$URL" --user-data-dir="$PROFILE" --class="$CLASS"
